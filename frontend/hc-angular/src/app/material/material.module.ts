import { NgModule } from '@angular/core';

const material = [
  // Add material components here
]

@NgModule({
  imports: [
    // Import components
  ],
  exports: [
    // export components
  ]
})
export class MaterialModule { }
